{
  "name": "@estimator3d/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic takeoff + pricing engine. Pure functions only.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
